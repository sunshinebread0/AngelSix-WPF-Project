<Window x:Class="WPF.Basics.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Basics"
        mc:Ignorable="d"
        Title="WPF Basics" Height="800" Width="400">
    <Border Padding="10">
        <StackPanel>
            <!-- Buttons -->
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="Apply" Grid.Column="0" Content="Apply" Margin="0 0 10 0"/>
                <Button x:Name="Reset" Grid.Column="1" Content="Reset"/>
                <Button Grid.Column="2" Content="Refresh" Margin="10 0 0 0"/>
                
            </Grid>

            <TextBlock  Text="Puls Properties" FontWeight="Bold" Margin="0 10"/>
            
            <!-- Description -->
            <TextBlock  Text= "Description" />
            <TextBox Text="{Binding Description}" Padding="2"/>
            
            <!--Status And Revision-->
            <Grid>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Status -->
                <StackPanel Grid.Column="0" Margin="0 0 10 0">
                    <TextBlock Text="Status"/>
                    <TextBox  IsReadOnly="True" Background="#eee" Padding="2"/>
                </StackPanel>
                
                <!-- Revision -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Revision"/>
                    <TextBox  IsReadOnly="True" Background="#eee" Padding="2"/>
                </StackPanel>
            </Grid>

            <!-- Part Number -->
            <StackPanel>
                <TextBlock Text="Part Number"/>
                <TextBox  IsReadOnly="True" Background="#eee" Padding="2"/>
            </StackPanel>
            
            <!-- Raw Material -->
            <TextBlock  Text="Raw Material" FontWeight="Bold" Margin="0 10"/>

            <!-- Material -->
            <TextBlock  Text="Material" />
            <ComboBox  Padding="2"/>

            <!-- Manufacturing Info -->
            <TextBlock  Text="Manufacturing Info" FontWeight="Bold" Margin="0 10"/>

            <!-- Work Centres -->
            <TextBlock  Text="Work Centres" Margin="0 0 0 10"/>
            <ItemsControl ItemsSource="{Binding WorkCenters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <!-- Length -->
            <TextBlock  Text="Length" />
            <TextBox Text="{Binding LengthText}" Padding="2"/>

            <!-- Mass -->
            <TextBlock Text="Mass"/>
            <TextBox Text="{Binding MassText}" IsReadOnly="True" Background="#eee" Padding="2"/>

            <!-- Finish -->
            <TextBlock  Text="Finish" />
            <ComboBox ItemsSource="{Binding FinishOptions}" SelectedItem="{Binding SelectedFinish}" Padding="2"/>

            <!-- Purchase Info -->
            <TextBlock  Text="Purchase Information" />
            <ComboBox SelectedIndex="0" Padding="2">
                <ComboBoxItem Content="Rubber"/>
                <ComboBoxItem Content="Not Rubber"/>
            </ComboBox>

            <!-- Supplier Name -->
            <TextBlock  Text="Supplier Name" />
            <TextBox  Text="{Binding SupplierText, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>

            <!-- Supplier Code -->
            <TextBlock  Text="Supplier Code" />
            <TextBox  Padding="2"/>

            <!-- Manufacturing Info -->
            <TextBlock  Text="Additional Info" FontWeight="Bold" Margin="0 10"/>

            <!-- Note -->
            <TextBlock  Text="Note" />
            <TextBox Text="{Binding NoteText}" Padding="2"/>
        </StackPanel>
    </Border>
</Window>
